import { AppelOffreReadModel } from './appelOffre.entity';

/**
 * Type temporaire en attendant une vraie implémentation d'Appel d'offre sous la forme d'un agrégat
 */
export type AppelOffreAggregate = AppelOffreReadModel;
