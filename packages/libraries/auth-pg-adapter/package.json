{
  "name": "@potentiel-libraries/auth-pg-adapter",
  "description": "Potentiel custom @auth/pg-adapter librairies rebundle with CommonJS support",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "prebuild": "tsc --emitDeclarationOnly --declaration --project tsconfig.json",
    "build": "esbuild src/index.ts --platform=node --bundle --format=cjs --outfile=dist/index.js"
  },
  "dependencies": {
    "@auth/pg-adapter": "1.8.0"
  },
  "devDependencies": {
    "@potentiel-config/tsconfig": "*"
  }
}
